services:
  oovah_app:
    build: .
    container_name: ${COMPOSE_PROJECT_NAME}
    ports:
      - "8008:8008"
    environment:
      - SQLITE_DB_PATH=/app/oovah.db
      - STATIC_PATH=web/dist
    networks:
      - app-network
    volumes:
      - .:/app

networks:
  app-network:
    driver: bridge
